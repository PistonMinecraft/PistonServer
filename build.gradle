ext {
    ENV = System.getenv()
    MC_VERSION = "1.16.5"
    libManifoldVersion = '2020.1.45'
}
subprojects {
    repositories {
        mavenCentral()
        maven { url = "https://libraries.minecraft.net" }
    }
    configurations {
        testImplementation.extendsFrom annotationProcessor
    }
    group = 'io.github.pistonmc'
}